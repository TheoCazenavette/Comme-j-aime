pipeline {
    agent any
    parameters {
        choice(name: 'SEXE', choices: ['Homme', 'Femme'], description: 'Veuillez indiquer votre sexe')
        string(name: 'NOM',  description: 'Veuillez indiquer votre nom')
        string(name: 'PRENOM',  description: 'Veuillez indiquer votre prénom')
        date(name: 'DDN',  description: 'Veuillez indiquer votre date de naissance')
        int(name:'TAILLE', description:'Veuillez indiquer votre taille (en cm)')
        int(name:'POIDS', description:'Veuillez indiquer votre poids (en kg)')
        booleanParam(name: 'SUBSCRIPTION', defaultValue: false, description: 'Voulez-vous vous abonner à notre programme de perte de poids?')
    }
    stages {
        stage('User Information') {
            steps {
                echo "Veuillez entrer vos informations:"
                echo " Votre nom : ${params.NOM}"
                echo " Votre prénom : ${params.PRENOM}"
                echo " Date de naissance : ${params.DDN}"
                echo " Votre poids : ${params.POIDS} kg"
                echo " Votre taille : ${params.TAILLE} cm"
                
                def gender = ""
                if (params.SEXE == "Femme") {
                    gender = "Mme."
                } else {
                    gender = "Mr."
                }
                echo "Bonjour ${gender} ${params.NOM} ${params.PRENOM} et bienvenue!"
                
                def subscriptionMessage = ""
                if (params.SUBSCRIPTION) {
                    subscriptionMessage = "Merci pour votre abonnement à notre programme de perte de poids!"
                } else {
                    subscriptionMessage = "Vous avez choisi de ne pas vous abonner à notre programme de perte de poids."
                }
                echo "${subscriptionMessage}"
            }
        }
        stage('Build') {
            steps {
                echo "Build"
                echo "Compiling US - Informations de l'utilisateur"
            }
        }
        stage('Test') {
            steps {
                echo "Test"
            }
        }
        stage('Deploy') {
            steps {
                echo "Deploy"
            }
        }
    }
}
